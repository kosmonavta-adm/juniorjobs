export const juniorsKeys = {
    all: ['juniors_all'],
};

export const peeksKeys = {
    all: ['peeks_all'],
    portfolio: () => [...peeksKeys.all, 'portfolio'],
};

export const portfoliosKeys = {
    single: ['portfolio_single'],
};

export const tagsKeys = {
    all: ['tags_all'],
};
